# Implements requirements for Notification Service core component configuration
NODE_ENV=development
PORT=3003
LOG_LEVEL=debug

# RabbitMQ Configuration
# Implements requirements for Event-Driven Architecture and Integration Layer
# Defines message queue settings for event-driven communication
RABBITMQ_URL=amqp://localhost:5672
WEBHOOK_QUEUE=webhook-delivery-queue
NOTIFICATION_EXCHANGE=notification-exchange
QUEUE_PREFETCH=10

# Webhook Configuration
# Implements requirements for Integration Layer
# Defines webhook delivery parameters for system integration
WEBHOOK_TIMEOUT_MS=5000
WEBHOOK_MAX_RETRIES=3
WEBHOOK_RETRY_DELAY_MS=60000